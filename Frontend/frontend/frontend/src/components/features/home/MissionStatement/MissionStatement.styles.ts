import { styled } from '@mui/material/styles';
import Typography from '@mui/material/Typography';

export const StyledMissionStatement = styled(Typography)(({ theme }) => ({
  // Add your mission statement styles here
}));
